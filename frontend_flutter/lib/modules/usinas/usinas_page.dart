import 'package:flutter/material.dart';

class UsinasPage extends StatelessWidget {
  const UsinasPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('CRUD de Usinas')));
}
