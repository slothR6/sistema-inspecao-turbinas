import 'package:flutter/material.dart';

class TurbinasPage extends StatelessWidget {
  const TurbinasPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('CRUD de Turbinas')));
}
